Installation:

	$ git clone https://github.com/brunoyb/dotfiles.git && cd dotfiles

Checkout Vim plugins:

	$ git submodule update --init

Create symlinks (Linux Bash):

	$ ln -s "$(pwd)/bash_aliases" ~/.bash_aliases
	$ ln -s "$(pwd)/bash_completion" ~/.bash_completion
	$ ln -s "$(pwd)/bash_completion.d" ~/.bash_completion.d
	$ ln -s "$(pwd)/bash_logout" ~/.bash_logout
	$ ln -s "$(pwd)/bash_profile" ~/.bash_profile
	$ ln -s "$(pwd)/bashrc" ~/.bashrc
	$ ln -s "$(pwd)/ctags" ~/.ctags
	$ ln -s "$(pwd)/emacs.d" ~/.emacs.d
	$ ln -s "$(pwd)/gvimrc" ~/.gvimrc
	$ ln -s "$(pwd)/inputrc" ~/.inputrc
	$ ln -s "$(pwd)/profile" ~/.profile
	$ ln -s "$(pwd)/vim" ~/.vim
	$ ln -s "$(pwd)/vimrc" ~/.vimrc
	$ ln -s "$(pwd)/Xresources" ~/.Xresources

Create symlinks (Windows PowerShell 5.0):

	$ New-Item -ItemType SymbolicLink -Path $home\.emacs.d -Target $pwd\emacs.d
	$ New-Item -ItemType SymbolicLink -Path $home\.inputrc -Target $pwd\inputrc
	$ New-Item -ItemType SymbolicLink -Path $home\vimfiles -Target $pwd\vim
	$ New-Item -ItemType SymbolicLink -Path $home\_vimrc -Target $pwd\vimrc
